# 💻 Controle de Abastecimento - Backend API

## 📝 Descrição

Este projeto consiste na aplicação **Backend** responsável por fornecer a **API** e 
a lógica de negócios para o sistema de abastecimentos de equipamentos em obras. 
É desenvolvido utilizando o framework **Spring Boot** para oferecer um serviço robusto e eficiente.

---

## 🚀 Tecnologias Utilizadas

O backend foi construído usando as seguintes tecnologias principais (inferidas pelo arquivo JAR):

* **Java** (JDK 17+): Linguagem de programação principal.
* **Spring Boot**: Framework para simplificar a criação de aplicações Java de nível de produção.
* **Spring Web MVC**: Para criação de APIs RESTful.
* **Persistência de Dados**: PostgreSQL
* **Outras Tecnologias**: Spring Data JPA, Lombok, Docker

---

## 🛠 Pré-requisitos

Para executar este projeto, você precisará ter instalado na sua máquina:

* **Java Development Kit (JDK) 17+** (A versão deve ser compatível com a compilação do projeto).
* **Docker**

---

## ⚙ Execução Local

O arquivo anexo (`backend-0.0.1-SNAPSHOT.jar`) é um **JAR executável** ("fat jar") do Spring Boot, o que significa que ele contém todas as dependências necessárias para ser executado diretamente.

### 1. Iniciar a Aplicação

Abra a linha de comando no diretório onde o arquivo `backend-0.0.1-SNAPSHOT.jar` está e execute:

```bash
java -jar backend-0.0.1-SNAPSHOT.jar